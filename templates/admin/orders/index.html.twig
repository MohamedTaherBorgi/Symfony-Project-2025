{% extends 'base.html.twig' %}

{% block title %}Manage Orders - Admin{% endblock %}

{% block body %}
<h1 class="mb-4">📦 Manage Orders</h1>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Order #</th>
                        <th>Customer</th>
                        <th>Amount</th>
                        <th>Payment</th>
                        <th>Status</th>
                        <th>Date</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for order in orders %}
                    <tr>
                        <td><strong>{{ order.orderNumber }}</strong></td>
                        <td>{{ order.user.prenom ~ ' ' ~ order.user.nom }}</td>
                        <td>{{ order.totalAmount }} TND</td>
                        <td>
                            <span class="badge bg-info">{{ order.paymentMethod|title|replace({'_': ' '}) }}</span>
                        </td>
                        <td>
                            <span class="badge bg-{{ order.status == 'delivered' ? 'success' : 'warning' }}">
                                {{ order.status|title }}
                            </span>
                        </td>
                        <td>{{ order.createdAt|date('d M Y') }}</td>
                        <td>
                            <a href="{{ path('admin_order_show', {id: order.id}) }}" 
                               class="btn btn-sm btn-eco">View</a>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="7" class="text-center">No orders yet</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}