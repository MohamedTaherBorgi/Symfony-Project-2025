{% extends 'base.html.twig' %}

{% block title %}All Products - EcoStore{% endblock %}

{% block body %}
<h1 class="text-center mb-5">🌱 All Eco-Friendly Products</h1>

<div class="row">
    {% for product in products %}
    <div class="col-md-3 mb-4">
        <div class="card product-card h-100">
            <div class="product-img">🌿</div>
            <div class="card-body d-flex flex-column">
                <span class="eco-badge mb-2">{{ product.category.nom }}</span>
                <h5 class="card-title">{{ product.nom }}</h5>
                <p class="card-text text-muted flex-grow-1">{{ product.description|slice(0, 60) }}...</p>
                <div class="d-flex justify-content-between align-items-center mt-auto">
                    <span class="h5 text-success mb-0">{{ product.prix }} TND</span>
                    <a href="{{ path('product_show', {id: product.id}) }}" class="btn btn-eco btn-sm">View</a>
                </div>
            </div>
        </div>
    </div>
    {% else %}
    <div class="col-12 text-center py-5">
        <p class="text-muted lead">No products found.</p>
    </div>
    {% endfor %}
</div>
{% endblock %}