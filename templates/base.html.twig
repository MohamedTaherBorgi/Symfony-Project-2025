<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}EcoStore - Sustainable Living{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Manrope:wght@300;400;500;600;700;800&family=Crimson+Pro:wght@300;400;600&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --forest-deep: #1a3a2e;
            --forest-dark: #2d5f4a;
            --sage-primary: #6b9080;
            --sage-light: #a4c3b2;
            --sand-warm: #eaf4f4;
            --cream: #f6fff8;
            --clay: #bc8c5d;
            --terracotta: #d4a574;
            --charcoal: #2c3531;
            --mist: #cce3de;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Manrope', -apple-system, sans-serif;
            background: var(--cream);
            color: var(--charcoal);
            line-height: 1.6;
            overflow-x: hidden;
            position: relative;
        }

        /* Organic background texture */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 20% 30%, rgba(107, 144, 128, 0.03) 0%, transparent 50%),
            radial-gradient(circle at 80% 70%, rgba(188, 140, 93, 0.03) 0%, transparent 50%),
            url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="300" height="300"><filter id="noise"><feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/></filter><rect width="300" height="300" filter="url(%23noise)" opacity="0.03"/></svg>');
            pointer-events: none;
            z-index: 0;
        }

        /* Navigation */
        .navbar {
            background: linear-gradient(to bottom, rgba(26, 58, 46, 0.98), rgba(26, 58, 46, 0.96));
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(107, 144, 128, 0.15);
            padding: 1.5rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
        }

        .navbar.scrolled {
            padding: 0.8rem 0;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
        }

        /* Brand logo */
        .navbar-brand {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            font-size: 1.75rem;
            color: var(--sand-warm) !important;
            letter-spacing: 0.5px;
            display: flex;
            align-items: center;
            gap: 12px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .brand-icon {
            width: 42px;
            height: 42px;
            background: linear-gradient(135deg, var(--sage-primary), var(--sage-light));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 4px 12px rgba(107, 144, 128, 0.3);
            transition: all 0.3s ease;
        }

        .navbar-brand:hover .brand-icon {
            transform: rotate(360deg) scale(1.1);
            box-shadow: 0 6px 20px rgba(107, 144, 128, 0.4);
        }

        /* Nav links */
        .nav-link {
            color: rgba(234, 244, 244, 0.9) !important;
            font-weight: 500;
            font-size: 0.95rem;
            padding: 0.6rem 1rem !important;
            border-radius: 10px;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 5px;
            left: 50%;
            width: 0;
            height: 2px;
            background: var(--sage-light);
            transform: translateX(-50%);
            transition: width 0.3s ease;
        }

        .nav-link:hover {
            color: white !important;
            background: rgba(107, 144, 128, 0.15);
            transform: translateY(-2px);
        }

        .nav-link:hover::after {
            width: 60%;
        }

        /* Profile image in nav */
        .nav-profile-img {
            width: 38px;
            height: 38px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(164, 195, 178, 0.3);
            margin-right: 8px;
            transition: all 0.3s ease;
        }

        .nav-link:hover .nav-profile-img {
            border-color: var(--sage-light);
            transform: scale(1.05);
        }

        /* Auth buttons */
        .login-btn {
            background: rgba(107, 144, 128, 0.15);
            color: var(--sand-warm) !important;
            font-weight: 600;
            border: 1.5px solid rgba(164, 195, 178, 0.3);
            transition: all 0.3s ease;
        }

        .login-btn:hover {
            background: rgba(107, 144, 128, 0.25);
            border-color: var(--sage-light);
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(107, 144, 128, 0.3);
        }

        .register-btn {
            background: linear-gradient(135deg, var(--clay), var(--terracotta));
            color: white !important;
            font-weight: 600;
            border: none;
            box-shadow: 0 4px 16px rgba(188, 140, 93, 0.3);
            transition: all 0.3s ease;
        }

        .register-btn:hover {
            background: linear-gradient(135deg, var(--terracotta), #e0b788);
            transform: translateY(-2px);
            box-shadow: 0 6px 24px rgba(188, 140, 93, 0.4);
        }

        /* Dropdown */
        .dropdown-menu {
            background: rgba(246, 255, 248, 0.98);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(107, 144, 128, 0.15);
            border-radius: 16px;
            box-shadow: 0 12px 48px rgba(0, 0, 0, 0.15);
            padding: 0.75rem;
            margin-top: 8px;
            animation: dropdownFade 0.3s ease;
        }

        @keyframes dropdownFade {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .dropdown-item {
            border-radius: 10px;
            padding: 0.7rem 1rem;
            font-weight: 500;
            color: var(--charcoal);
            transition: all 0.2s ease;
        }

        .dropdown-item:hover {
            background: linear-gradient(90deg, rgba(107, 144, 128, 0.08), transparent);
            color: var(--forest-dark);
            transform: translateX(4px);
        }

        .dropdown-divider {
            border-color: rgba(107, 144, 128, 0.15);
            margin: 0.5rem 0;
        }

        .dropdown-header {
            color: var(--sage-primary);
            font-weight: 700;
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            padding: 0.6rem 1rem 0.3rem;
        }

        /* Main content wrapper */
        main {
            position: relative;
            z-index: 1;
            min-height: 60vh;
        }

        /* Premium buttons */
        .btn-eco {
            background: linear-gradient(135deg, var(--sage-primary), var(--forest-dark));
            color: white;
            border: none;
            border-radius: 12px;
            padding: 0.75rem 1.75rem;
            font-weight: 600;
            letter-spacing: 0.3px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 4px 16px rgba(107, 144, 128, 0.3);
            position: relative;
            overflow: hidden;
        }

        .btn-eco::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .btn-eco:hover::before {
            left: 100%;
        }

        .btn-eco:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 24px rgba(107, 144, 128, 0.4);
            color: white;
        }

        /* Cards */
        .card {
            border: none;
            border-radius: 20px;
            background: white;
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.06);
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .card:hover {
            box-shadow: 0 12px 48px rgba(0, 0, 0, 0.1);
            transform: translateY(-4px);
        }

        /* Product cards */
        .product-card {
            border-radius: 20px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 16px 56px rgba(0, 0, 0, 0.12);
        }

        .product-card img {
            border-radius: 20px 20px 0 0;
            transition: transform 0.4s ease;
        }

        .product-card:hover img {
            transform: scale(1.05);
        }

        /* Badges */
        .eco-badge {
            background: linear-gradient(135deg, var(--clay), var(--terracotta));
            color: white;
            padding: 0.4rem 0.9rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            display: inline-block;
            box-shadow: 0 2px 8px rgba(188, 140, 93, 0.3);
        }

        /* Alerts */
        .alert {
            border: none;
            border-radius: 16px;
            padding: 1rem 1.25rem;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
            animation: slideIn 0.4s ease;
            margin-bottom: 1rem;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-20px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .alert-success {
            background: linear-gradient(135deg, rgba(107, 144, 128, 0.15), rgba(164, 195, 178, 0.15));
            color: var(--forest-dark);
            border-left: 4px solid var(--sage-primary);
        }

        .alert-danger {
            background: linear-gradient(135deg, rgba(220, 53, 69, 0.15), rgba(220, 53, 69, 0.1));
            color: #721c24;
            border-left: 4px solid #dc3545;
        }

        .alert-warning {
            background: linear-gradient(135deg, rgba(212, 165, 116, 0.15), rgba(188, 140, 93, 0.1));
            color: #856404;
            border-left: 4px solid var(--clay);
        }

        .alert-info {
            background: linear-gradient(135deg, rgba(107, 144, 128, 0.1), rgba(164, 195, 178, 0.1));
            color: var(--forest-dark);
            border-left: 4px solid var(--sage-light);
        }

        /* Forms */
        .form-control, .form-select {
            border: 2px solid rgba(107, 144, 128, 0.2);
            border-radius: 12px;
            padding: 0.75rem 1rem;
            transition: all 0.3s ease;
            background: white;
        }

        .form-control:focus, .form-select:focus {
            border-color: var(--sage-primary);
            box-shadow: 0 0 0 4px rgba(107, 144, 128, 0.1);
            outline: none;
        }

        .form-label {
            font-weight: 600;
            color: var(--forest-dark);
            font-size: 0.9rem;
            letter-spacing: 0.3px;
            margin-bottom: 0.5rem;
        }

        /* Hero section */
        .hero {
            background: linear-gradient(135deg,
            rgba(107, 144, 128, 0.08) 0%,
            rgba(164, 195, 178, 0.08) 100%);
            border-radius: 32px;
            padding: 5rem 3rem;
            margin: 3rem 0;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -30%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(107, 144, 128, 0.1) 0%, transparent 70%);
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% {
                transform: translate(0, 0) rotate(0deg);
            }
            33% {
                transform: translate(-30px, 30px) rotate(120deg);
            }
            66% {
                transform: translate(30px, -20px) rotate(240deg);
            }
        }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            font-weight: 800;
            color: var(--forest-deep);
            margin-bottom: 1.5rem;
            line-height: 1.1;
            position: relative;
            z-index: 1;
        }

        .hero-subtitle {
            font-size: 1.25rem;
            color: var(--charcoal);
            font-weight: 400;
            line-height: 1.7;
            margin-bottom: 2.5rem;
            position: relative;
            z-index: 1;
        }

        /* Section titles */
        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            font-weight: 700;
            color: var(--forest-deep);
            margin: 3rem 0 2rem;
            text-align: center;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -12px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--sage-primary), transparent);
            border-radius: 2px;
        }

        /* Footer */
        footer {
            background: linear-gradient(to top, var(--forest-deep), var(--forest-dark));
            color: var(--sand-warm);
            padding: 3rem 0 2rem;
            margin-top: 5rem;
            position: relative;
            overflow: hidden;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg,
            transparent,
            rgba(164, 195, 178, 0.3),
            transparent);
        }

        footer p {
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }

        footer small {
            opacity: 0.8;
        }

        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--sand-warm);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--sage-primary), var(--forest-dark));
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, var(--sage-light), var(--sage-primary));
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero {
                padding: 3rem 1.5rem;
                border-radius: 24px;
            }

            .hero-title {
                font-size: 2.5rem;
            }

            .section-title {
                font-size: 2rem;
            }
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>

<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <div class="brand-icon">🌿</div>
            EcoStore
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">
                        <i class="bi bi-house-door"></i> Home
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-box-seam"></i> Products
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="{{ path('product_index') }}">
                                <i class="bi bi-grid"></i> All Products
                            </a>
                        </li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li class="dropdown-header">Categories</li>
                        {% for cat in categories %}
                            <li>
                                <a class="dropdown-item" href="{{ path('product_by_category', { id: cat.id }) }}">
                                    {{ cat.nom }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('cart_index') }}">
                            <i class="bi bi-cart"></i> Cart
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('order_my_orders') }}">
                            <i class="bi bi-receipt"></i> Orders
                        </a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">
                                <i class="bi bi-gear"></i> Admin
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button"
                           data-bs-toggle="dropdown">
                            {% if app.user.image %}
                                <img src="{{ asset('uploads/users/' ~ app.user.image) }}" class="nav-profile-img"
                                     alt="{{ app.user.prenom }}">
                            {% endif %}
                            {{ app.user.prenom }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="{{ path('app_profile_edit') }}">
                                    <i class="bi bi-person-circle"></i> Profile
                                </a>
                            </li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right"></i> Logout
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item ms-2">
                        <a href="{{ path('app_login') }}" class="nav-link rounded-pill px-4 login-btn">
                            Login
                        </a>
                    </li>
                    <li class="nav-item ms-2">
                        <a href="{{ path('app_register') }}" class="nav-link rounded-pill px-4 register-btn">
                            Register
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<main class="container my-4">
    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-circle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('warning') %}
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('info') %}
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            <i class="bi bi-info-circle-fill me-2"></i>{{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% block body %}{% endblock %}
</main>

<footer class="text-center">
    <div class="container">
        <p class="mb-2">🌍 EcoStore - Sustainable Living Made Simple</p>
        <small>Crafted with care for our planet 💚</small>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Add scroll effect to navbar
    window.addEventListener('scroll', function () {
        const navbar = document.querySelector('.navbar');
        if (window.scrollY > 50) {
            navbar.classList.add('scrolled');
        } else {
            navbar.classList.remove('scrolled');
        }
    });
</script>
{% block javascripts %}{% endblock %}
</body>
</html>
